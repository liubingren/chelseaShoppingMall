class Point {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }

  toString() {
    return '(' + this.x + ', ' + this.y + ')';
  }

  funA(){
  	return 'dk';
  }
  
}

export var p = new Point(1,22);